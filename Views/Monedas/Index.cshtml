@model IEnumerable<MicroLabsFinal.Models.Moneda>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tipoMoneda)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tipoMoneda)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.monedaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.monedaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.monedaID })
        </td>
    </tr>
}

</table>
