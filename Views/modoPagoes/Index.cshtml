@model IEnumerable<MicroLabsFinal.Models.modoPago>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tipopPago)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tipopPago)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.pagoID }) |
            @Html.ActionLink("Details", "Details", new { id=item.pagoID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.pagoID })
        </td>
    </tr>
}

</table>
